<?php
if(!defined('__MAPC__')) { exit(); }

require(INIT_PATH . 'init.core.php');
{ // MODEL : Start

} // MODEL : Finish


{ // View : Start

    $VIEW['layout_file'] = 'html_main.view.php';

} // View : Finish

// this is it
